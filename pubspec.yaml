name: provider_assist
description: Extras for the provider package to enhance the pattern
version: 0.0.1
author: Ryan Dixon
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
